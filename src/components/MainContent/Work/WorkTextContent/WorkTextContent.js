import styles from "./WorkTextContent.module.scss";

const WorkTextContent = () => {
  return (
    <div className={styles.workTextContent}>
      <h2>work</h2>
      <p>Some of my projects so far.</p>
    </div>
  );
};

export default WorkTextContent;
